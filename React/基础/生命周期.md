```jsx
class Clock extends React.Component {
  constructor(props) {
    super(props);
  }
  componentDidMount() { //挂载
  	# 在组件已经被渲染到 DOM 中后运行
  }
  componentWillUnmount() { // 卸载
    // 清除计时器
  
  }
  componentDidUpdate(){
    
  }
  render() {
    return ();
  }
}
```
